<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>es.meyss.sgtic</groupId>
	<artifactId>cooperativas</artifactId>
	<packaging>pom</packaging>
	<version>0.1.4</version>
	<name>${project.artifactId}</name>
	<description>Componente ${project.artifactId}</description>
	<modules>
		<module>cooperativasISRV</module>
		<module>cooperativasSRV</module>
		<module>cooperativasWAR</module>
	</modules>
	
	<properties>
		<!-- poner a false para ejecutar test -->
		<maven.test.skip>true</maven.test.skip>
		
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<java.version>1.7</java.version>
		
		<slf4j.version>1.7.7</slf4j.version>
		<spring.version>4.3.5.RELEASE</spring.version>
		<spring.security.version>3.2.8.RELEASE</spring.security.version>
		<hibernate.version>4.2.20.Final</hibernate.version>
		<hibernate-validator.version>5.4.0.Final</hibernate-validator.version>
		<dozer.version>5.3.2</dozer.version>
		<tiles.version>2.2.2</tiles.version>
		<oracle.jdbc.version>6</oracle.jdbc.version>
		<junit.version>4.8.2</junit.version>
		<commons.dbpc.version>1.2.2</commons.dbpc.version>
		<xalan.version>2.7.2</xalan.version>
		<xerces.version>2.10.0</xerces.version>
		<xml.resolver.version>1.2</xml.resolver.version>
		<xml.apis.version>1.4.01</xml.apis.version>
		<log4j.version>1.2.17</log4j.version>	
		<sgtic.common.version>1.1.7</sgtic.common.version>
		<commons.lang.version>2.6</commons.lang.version>
		<commons.beanutils.version>1.9.2</commons.beanutils.version>
		<commons.collection.version>3.2.1</commons.collection.version>
		<aspectj.version>1.7.1</aspectj.version>
		<anotame.version>1.0.2</anotame.version>	
		<javax.validation.version>1.1.0.Final</javax.validation.version>
		<itext.version>2.1.7</itext.version>
		<poi.version>3.10.1</poi.version>
		<commons.fileupload.version>1.3.2</commons.fileupload.version>
		<javax.servlet.version>3.0.1</javax.servlet.version>
		<jsp.api.version>2.2</jsp.api.version>
		<standard.taglibs.version>1.1.2</standard.taglibs.version>
		<javax.servlet.jstl.version>1.2</javax.servlet.jstl.version>
		<json.version>2.9.4</json.version>
		<candela.version>1.0.3</candela.version>
		<clave.version>1.1.0</clave.version>
		<jsoup.version>1.10.3</jsoup.version>
		<reflections.version>0.9.11</reflections.version>
		<dandelion.datatables.version>1.1.0</dandelion.datatables.version>
		
		<warOutputDirectory>C:/warAplicaciones/${project.parent.artifactId}/${project.version}/${entorno}</warOutputDirectory>
	</properties>
	
	<repositories>
		<repository>
			<id>PRE - XAPMP - Artifactory</id>
			<name>PRE - XAPMP - Artifactory-releases</name>
			<url>http://vmxapmp/artifactory/repo</url>
		</repository>
	</repositories>
	
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>2.3.2</version>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<version>2.4.3</version>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!-- Local de jboss -->
		<profile>
			<id>local-jboss</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<properties>
				<entorno>local</entorno>
      		</properties>
      		<build>
      			<finalName>${project.parent.artifactId}-${project.version}-${entorno}</finalName>
      			<resources>
		     		<resource>
		       			<directory>src/main/resources/local</directory>
		     		</resource>
		     		<resource>
		       			<directory>src/main/resources</directory>
		       			<filtering>true</filtering>
		       			<includes>
							<include>i18n/*.properties</include>
	        			</includes>
		     		</resource>
					<resource>
		       			<directory>src/main/resources</directory>
		       			<filtering>false</filtering>
		       			<includes>
		       				<include>*.xml</include>
	          				<include>config/**/*.xml</include>
	          				<include>doc/*.*</include>
							<include>i18n/*.jpg</include>
							<include>i18n/*.gif</include>
							<include>propertyFiles/*.*</include>
							<include>dandelion/*.properties</include>
	          				<include>dandelion/datatables/*.properties</include>
	        			</includes>
		     		</resource>
		   		</resources>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-war-plugin</artifactId>
						<version>2.1.1</version>
						<configuration>
 						    <!-- <warSourceExcludes>css/**,fonts/**,img/**,js/**</warSourceExcludes> -->
							<outputDirectory>${warOutputDirectory}</outputDirectory>							
						</configuration>
					</plugin>
				</plugins>
			</build>
			<dependencies>
				<!-- log -->
        		<dependency>
            		<groupId>org.slf4j</groupId>
            		<artifactId>slf4j-log4j12</artifactId>
            		<version>${slf4j.version}</version>
        		</dependency>
			
				<dependency>
					<groupId>org.hibernate</groupId>
					<artifactId>hibernate-core</artifactId>
					<version>${hibernate.version}</version>
					<scope>provided</scope>
					<exclusions>
			      		<exclusion>
			         		<groupId>antlr</groupId>
			         		<artifactId>antlr</artifactId>
			      		</exclusion>
			   		</exclusions>
				</dependency>
				<dependency>
					<groupId>org.hibernate</groupId>
					<artifactId>hibernate-entitymanager</artifactId>
					<version>${hibernate.version}</version>
					<scope>provided</scope>
					<exclusions>
			      		<exclusion>
			         		<groupId>antlr</groupId>
			         		<artifactId>antlr</artifactId>
			      		</exclusion>
			   		</exclusions>
				</dependency>
			</dependencies>
		</profile>

		<!-- PROFILE DESARROLLO -->
		<profile>
			<id>des</id>
			<properties>
				<entorno>des</entorno>
      		</properties>
      		
      		<build>
      			<finalName>${project.parent.artifactId}-${project.version}-${entorno}</finalName>
      			<resources>
		     		<resource>
		       			<directory>src/main/resources/des</directory>
		     		</resource>
		     		<resource>
		       			<directory>src/main/resources</directory>
		       			<filtering>true</filtering>
		       			<includes>
							<include>i18n/*.properties</include>
	        			</includes>
		     		</resource>
					<resource>
		       			<directory>src/main/resources</directory>
		       			<filtering>false</filtering>
		       			<includes>
		       				<include>*.xml</include>
	          				<include>config/**/*.xml</include>
	          				<include>doc/*.*</include>
							<include>i18n/*.jpg</include>
							<include>i18n/*.gif</include>
							<include>propertyFiles/*.*</include>
	          				<include>dandelion/*.properties</include>
	          				<include>dandelion/datatables/*.properties</include>
	        			</includes>
		     		</resource>
		   		</resources>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-war-plugin</artifactId>
						<version>2.1.1</version>
						<configuration>
							<warSourceExcludes>css/**,fonts/**,img/**,js/**</warSourceExcludes>
							<outputDirectory>${warOutputDirectory}</outputDirectory>							
						</configuration>
					</plugin>
				</plugins>
			</build>
			<dependencies>
				<!-- log -->
        		<dependency>
            		<groupId>org.slf4j</groupId>
            		<artifactId>slf4j-log4j12</artifactId>
            		<version>${slf4j.version}</version>
        		</dependency>
                
                <!-- hibernate -->
				<dependency>
					<groupId>org.hibernate</groupId>
					<artifactId>hibernate-core</artifactId>
					<version>${hibernate.version}</version>
					<scope>provided</scope>
				</dependency>
				<dependency>
					<groupId>org.hibernate</groupId>
					<artifactId>hibernate-entitymanager</artifactId>
					<version>${hibernate.version}</version>
					<scope>provided</scope>
				</dependency>
			</dependencies>
		</profile>
		
		<!-- PROFILE PREPRODUCCION -->
		<profile>
			<id>pre</id>
			<properties>
				<entorno>pre</entorno>
      		</properties>
      		<build>
      			<finalName>${project.parent.artifactId}-${project.version}-${entorno}</finalName>
      			<resources>
		     		<resource>
		       			<directory>src/main/resources/pre</directory>
		     		</resource>
		     		<resource>
		       			<directory>src/main/resources</directory>
		       			<filtering>true</filtering>
		       			<includes>
							<include>i18n/*.properties</include>
	        			</includes>
		     		</resource>
					<resource>
		       			<directory>src/main/resources</directory>
		       			<filtering>false</filtering>
		       			<includes>
		       				<include>*.xml</include>
	          				<include>config/**/*.xml</include>
	          				<include>doc/*.*</include>
							<include>i18n/*.jpg</include>
							<include>i18n/*.gif</include>
							<include>propertyFiles/*.*</include>
	          				<include>dandelion/*.properties</include>
	          				<include>dandelion/datatables/*.properties</include>
	        			</includes>
		     		</resource>
		   		</resources>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-war-plugin</artifactId>
						<version>2.1.1</version>
						<configuration>
							<packagingExcludes>WEB-INF/web.xml</packagingExcludes>
							<warSourceExcludes>css/**,fonts/**,img/**,js/**</warSourceExcludes>
							<outputDirectory>${warOutputDirectory}</outputDirectory>							
						</configuration>
					</plugin>
				</plugins>
			</build>
			<dependencies>
				<!-- log -->
        		<dependency>
            		<groupId>org.slf4j</groupId>
            		<artifactId>slf4j-log4j12</artifactId>
            		<version>${slf4j.version}</version>
        		</dependency>
                
                <!-- hibernate -->
				<dependency>
					<groupId>org.hibernate</groupId>
					<artifactId>hibernate-core</artifactId>
					<version>${hibernate.version}</version>
					<scope>provided</scope>
				</dependency>
				<dependency>
					<groupId>org.hibernate</groupId>
					<artifactId>hibernate-entitymanager</artifactId>
					<version>${hibernate.version}</version>
					<scope>provided</scope>
				</dependency>
			</dependencies>
		</profile>
		
		<!-- PROFILE PRODUCCION -->
		<profile>
			<id>pro</id>
			<properties>
				<entorno>pro</entorno>
      		</properties>
      		<build>
      			<finalName>${project.parent.artifactId}-${project.version}-${entorno}</finalName>
      			<resources>
		     		<resource>
		       			<directory>src/main/resources/pro</directory>
		     		</resource>
		     		<resource>
		       			<directory>src/main/resources</directory>
		       			<filtering>true</filtering>
		       			<includes>
							<include>i18n/*.properties</include>
	        			</includes>
		     		</resource>
					<resource>
		       			<directory>src/main/resources</directory>
		       			<filtering>false</filtering>
		       			<includes>
		       				<include>*.xml</include>
	          				<include>config/**/*.xml</include>
	          				<include>doc/*.*</include>
							<include>i18n/*.jpg</include>
							<include>i18n/*.gif</include>
							<include>propertyFiles/*.*</include>
	          				<include>dandelion/*.properties</include>
	          				<include>dandelion/datatables/*.properties</include>
	        			</includes>
		     		</resource>
		   		</resources>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-war-plugin</artifactId>
						<version>2.1.1</version>
						<configuration>
							<packagingExcludes>WEB-INF/web.xml</packagingExcludes>
							<warSourceExcludes>css/**,fonts/**,img/**,js/**</warSourceExcludes>
							<outputDirectory>${warOutputDirectory}</outputDirectory>							
						</configuration>
					</plugin>
				</plugins>
			</build>
			<dependencies>
				<!-- log -->
        		<dependency>
            		<groupId>org.slf4j</groupId>
            		<artifactId>slf4j-log4j12</artifactId>
            		<version>${slf4j.version}</version>
        		</dependency>
                
                <!-- hibernate -->
				<dependency>
					<groupId>org.hibernate</groupId>
					<artifactId>hibernate-core</artifactId>
					<version>${hibernate.version}</version>
					<scope>provided</scope>
				</dependency>
				<dependency>
					<groupId>org.hibernate</groupId>
					<artifactId>hibernate-entitymanager</artifactId>
					<version>${hibernate.version}</version>
					<scope>provided</scope>
				</dependency>
			</dependencies>
		</profile>
	</profiles>
</project>
